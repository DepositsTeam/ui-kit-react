import PropTypes from "prop-types";
import Box from "../box";
import Text from "../text";

const Table = ({ ...props }) => {
  return <Box />;
};

export default Table;

Table.propTypes = {};

Table.defaultProps = {};
