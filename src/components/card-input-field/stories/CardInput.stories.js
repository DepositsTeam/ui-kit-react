import React from "react";
import CardInputField from "../src/CardInputField";

export default {
  title: "Card Input Field",
  component: CardInputField,
};

const Template = (args) => <CardInputField {...args} />;

export const Default = Template.bind({})